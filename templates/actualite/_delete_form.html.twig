<div class="container">
    <form method="post" action="{{ path('news_actualite_delete', {'id': actualite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_method" value="DELETE">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actualite.id) }}">
        <button class="btn">Delete</button>
    </form>

</div>